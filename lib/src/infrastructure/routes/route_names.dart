import 'route_path.dart';

class RouteNames {
  static const String todo = RoutePath.todo;
  static const String addTodo = '${RoutePath.todo}${RoutePath.addTodo}';
  static const String editTodo = '${RoutePath.todo}${RoutePath.editTodo}';
}
